Hi, <%= params[:user].email %>

Someone requested a reset of your password.

If this was you, click the link below to reset you password. If was not, just ignore. The link will expire automatically in 15 minutes.

<%= password_reset_edit_url(token: @token) %>
